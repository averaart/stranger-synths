import * as React from 'react';
import * as ReactDOM from 'react-dom';
import * as App from './components/App';
import { WORKAROUND } from "react-rangeslider"

console.log( WORKAROUND );

App.renderDestination('root')